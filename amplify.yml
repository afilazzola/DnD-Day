version: 1
frontend:
  buildpath: src
  phases:
    preBuild:
      commands:
        - nvm install 22
        - nvm use 22
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: src/dist
    files:
      - "**/*"
  cache:
    paths:
      - src/node_modules/**/*
